/**
 * Barrel — point d'import stable `@/features/interventions/types`
 * référencé par 350+ fichiers. Ne pas réagencer sans plan de migration.
 *
 * Types définis dans `./types/*` : `Intervention`, `InterventionEvent`.
 */
export type { InterventionEvent } from "./types/event";
export type { Intervention } from "./types/intervention";
