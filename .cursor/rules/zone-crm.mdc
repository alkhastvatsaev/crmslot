---
description: Zone C — Historique CRM
globs: src/features/crmHistory/**,src/app/api/ai/crm-history-agent/**
alwaysApply: false
---

# Zone CRM History

**Tests** : `npm run test:crm` → `npm run test:ci` avant merge.

Timeline : `interventions/{id}/timeline`. Agents hub dans `src/features/hubAgents/` si intent CRM.
