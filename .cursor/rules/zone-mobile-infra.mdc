---
description: Zone F-infra — détection mobile, PWA, gate desktop
globs: src/features/mobile/**,src/features/app/**,src/core/config/mobile*.ts,src/core/ui/**/useIsMobile.ts,tests/e2e/desktop-only-gate-prod.spec.ts,tests/e2e/mobile-shell.spec.ts,tests/e2e/api-mobile-config.spec.ts
alwaysApply: false
---

# Mobile infra (Cursor)

**Tests** : `npm run test:mobile-infra` ; E2E : `npm run test:e2e:mobile`, `npm run test:e2e:desktop-gate`.

Fichiers : `useIsMobile.ts`, `mobileAccess.ts`, `DesktopOnlyGate.tsx`, `GET /api/mobile/config`.

**Ne pas** modifier le shell UI mobile (`MobileTopBar`, CSS layout) — zone F-UI = Claude Code.

i18n : clés uniquement si demandées explicitement.
