---
description: Interventions P0 — assignation, planning technicien
globs: src/features/interventions/**
alwaysApply: false
---

# Zone Interventions (P0)

**Tests** : `npm run test:interventions` — seuils P0 dans `jest.config.ts` (`assignInterventionToTechnician`, `technicianSchedule`, `mapTechnicianMissions`, etc.).

Serveur : `src/features/interventions/server/*.ts` — tests `@jest-environment node` avec mocks Admin.

UI assignation : `data-testid` obligatoires pour tests RTL.
